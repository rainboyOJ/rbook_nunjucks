Help the student write or improve `brute.cpp`.

Arguments: `$ARGUMENTS`

Use the `oj-student-coach` and `oj-cpp-rbook-style` skills.

Read `problem.md` and existing `brute.cpp` if present. The goal is a clear small-data solution with the same input/output format as `main.cpp`.

Allowed:

- write or rewrite `brute.cpp`;
- explain the brute-force complexity;
- point out what data range it can verify;
- make it suitable for `oj duipai`.

Not allowed:

- jump directly to optimized `main.cpp`;
- hide key logic behind advanced C++ tricks.

After editing, suggest running:

```bash
.opencode/oj-tools/bin/oj sample
```
