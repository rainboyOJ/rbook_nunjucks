Compile and run `main.cpp` for the current problem.

Arguments: `$ARGUMENTS`

Run:

```bash
.opencode/oj-tools/bin/oj run $ARGUMENTS
```

If compilation or runtime fails, explain the error briefly and point to the likely file/function/line area. Do not replace the whole solution unless the student explicitly asks for a review.
