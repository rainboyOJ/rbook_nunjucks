---
id: "max-subarray-monotonic-queue"
title: "最大子序和"
date: 2026-06-16 00:00
toc: true
tags: ["单调队列", "前缀和"]
categories: ["数据结构"]
code_template:
  - title: 最大子序和
    desc: "前缀和加单调队列求长度不超过 m 的最大子段和"
    tags: ["单调队列", "前缀和"]
    code: /code/data-struture/monotonic_queue_max_subarray.cpp
---

[[TOC]]

## 一句话算法

长度受限的最大子段和，就是固定右端点时，在合法左端点里找最小前缀和。

## 问题模型

给定序列 $a_1,a_2,\ldots,a_n$ 和长度限制 $m$，求长度不超过 $m$ 的连续子段最大和。

设前缀和为：

$$
S_i = a_1+a_2+\cdots+a_i
$$

区间 $(j+1,i)$ 的和为：

$$
S_i-S_j
$$

当右端点为 $i$ 时，左端点前缀下标 $j$ 必须满足：

$$
i-m \le j \le i-1
$$

所以要最大化 $S_i-S_j$，只需要在这个范围里找最小的 $S_j$。

## 核心直觉

右端点 $i$ 从左到右移动时，合法的 $j$ 范围也是一个滑动窗口：

```text
[i-m, i-1]
```

我们维护这个窗口里的最小前缀和下标，队头就是当前最优左端点。

## 代码实现

@include-code(/code/data-struture/monotonic_queue_max_subarray.cpp, cpp)

## 测试用例

输入：

```text
6 3
1 -2 3 5 -1 2
```

输出：

```text
8
```

解释：长度不超过 `3` 的最大子段是 `3 5`，和为 `8`。

## 经典例题

### 1. [AcWing 135](https://www.acwing.com/problem/content/137/)

最大子序和模板题。重点是把“长度不超过 $m$”变成前缀和下标窗口。

### 2. [[problem: luogu,P1714]]

同一模型。枚举右端点，单调队列维护合法范围内的最小前缀和。
