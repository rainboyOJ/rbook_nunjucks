TODO 对Sparse 的补充
前趋: 倍增思想, Sparese table, 图论DAG知识.
后继: tree jump 求LCA


DAG jump

function jump

- 在DAG上的最快跳跃问题
-

用到了个DP知识

$$

$$


1. 求每个点i走x步能到的最远点
2. 求不超过后继点x的LEVEL后,能到达的最远点是哪个点,且能到达的点是那个点?

最后总结一下,哪些可以用`Sparse table`存信息

1. 链上区间最值
2. 链上的快速跳转
    2. 函数复合
    3. 关系图,每个点的出度为1 !!!(其实就是函数),在这样的图上求$f(i,2^j)$的复合信息.

!!! 核心:

证明在链上种跳转一定可以达到目标的前一个位置.!!!


- 证明: 最有路径是唯一的,通过每个点都有唯一的出发点得到


适用:

每人有点唯一的
- o(1)判断某个位置是否是可达的(二分性)
- 求前面可达位置的数量
- 求最远可达点.
